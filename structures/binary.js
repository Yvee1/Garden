function setup() {
  if (windowWidth<640){

  }
  else{

  }
  canvas = createCanvas(__, __);
  // Move the canvas so it’s inside our <div id="sketch-holder">.
  canvas.parent('sketch-holder');
}

function windowResized() {
  // Resize canvas if window is resized
  if (windowWidth<640){

  }
  else{

  }
  resizeCanvas(__, __);
  // Move the canvas so it’s inside our <div id="sketch-holder">.
  canvas.parent('sketch-holder');
}

function draw() {
  // To be a binary tree
}
